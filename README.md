Two ways to make a word grid.

1. Set the grid size, fill with random letters, use search to identify words.
2. Set the words, computer places them (grid grows as needed), fills rest of grid with random letters
